[retrieval]
semantic_top_k = 20
max_l2_distance = 0.75
retrieval_limit = 20
reranker_min_score = 0.01

[storage]
lancedb_file = "substrate.lancedb"
sqlite_file = "substrate.sqlite"

[http]
port = 4004

[logging]
level = "debug"
